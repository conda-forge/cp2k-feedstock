--- src/mpiwrap/message_passing.F.bak	2026-05-27 14:08:26
+++ src/mpiwrap/message_passing.F	2026-05-27 14:08:53
@@ -62,7 +62,19 @@
 #if defined(__parallel)
 ! subroutines: unfortunately, mpi implementations do not provide interfaces for all subroutines (problems with types and ranks explosion),
 !              we do not quite know what is in the module, so we can not include any....
-!              to nevertheless get checking for what is included, we use the mpi module without use clause, getting all there is
+!              to nevertheless get checking for what is included, we use the mpi module with ONLY clause
+#if defined(__MPI_F08)
+   USE mpi_f08, ONLY : MPI_Get_address, MPI_Comm_rank, MPI_Comm_size, &
+                       MPI_Comm_split, MPI_Comm_free, MPI_Comm_create, &
+                       MPI_Comm_dup, MPI_Barrier, MPI_Allreduce, &
+                       MPI_Reduce_scatter, MPI_Alltoallv, MPI_Allgatherv, &
+                       MPI_Alltoall, MPI_Bcast, MPI_Waitall, MPI_Isend, &
+                       MPI_Irecv, MPI_Type_create_struct, MPI_Type_commit, &
+                       MPI_Type_free, MPI_Address, MPI_Status, MPI_Request, &
+                       MPI_Datatype, MPI_Errhandler, MPI_Comm, MPI_Win, &
+                       MPI_File, MPI_Info, MPI_Group, MPI_Wtime, MPI_Wtick, &
+                       MPI_Initialized, MPI_Abort, MPI_Comm_group, MPI_Group_incl, MPI_Group_free
+#endif
 #if defined(__MPI_F08)
    USE mpi_f08
 #else
@@ -3991,11 +4003,21 @@
          INTEGER, ALLOCATABLE, DIMENSION(:)         :: lengths
 #if defined(__parallel)
          INTEGER :: ierr
-         INTEGER(kind=mpi_address_kind), &
-            ALLOCATABLE, DIMENSION(:)               :: displacements
+          INTEGER(kind=mpi_address_kind), &
+             ALLOCATABLE, DIMENSION(:)               :: displacements
+#if defined(__MPI_F08) && !defined(__GNUC__)
+         INTERFACE
+            SUBROUTINE MPI_Get_address(location, address, ierror) BIND(C, NAME="MPI_Get_address")
+               USE, INTRINSIC :: ISO_C_BINDING, ONLY: C_PTR, C_INT
+               TYPE(C_PTR), VALUE :: location
+               INTEGER(KIND=MPI_ADDRESS_KIND), INTENT(OUT) :: address
+               INTEGER, INTENT(OUT) :: ierror
+            END SUBROUTINE MPI_Get_address
+         END INTERFACE
+#endif
 #if defined(__MPI_F08)
-         ! Even OpenMPI 5.x misses mpi_get_address in the F08 interface
-         EXTERNAL                                   :: mpi_get_address
+          ! Even OpenMPI 5.x misses mpi_get_address in the F08 interface
+          EXTERNAL                                   :: mpi_get_address
 #endif
 #endif
          MPI_DATA_TYPE, ALLOCATABLE, DIMENSION(:) :: old_types
